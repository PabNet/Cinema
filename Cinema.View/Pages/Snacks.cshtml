@page
@model Cinema.View.Pages.SnacksModel

@{
    ViewData["Title"] = Model.Title;
}

<div class="line-hor"></div>
<div class="box">
    <div class="border-right">
        <div class="border-left">
            <div class="inner">
                <h3 style="text-align: center">Список <span>еды</span></h3>
                <div style="margin-left: 43%;" class="wrapper">
                    <a href="/SnackForm/" class="link2">
                        <span>
                            <span>Добавить</span>
                        </span>
                    </a>
                </div>
                @if (Model.Snacks.Count != default)
                {
                    <ul class="list">
                        @foreach (var snack in Model.Snacks)
                        {
                            <li>
                                <a>@snack.Name</a><br/>
                                @snack.Description
                                <div>
                                    <a href="/SnackForm/?id=@snack.Id" class="link2" style="margin-right: 20px">
                                        <span>
                                            <span class="button">Изменить</span>
                                        </span>
                                    </a>
                                </div>
                                <div>
                                    <a href="/Snacks/?handler=Delete&id=@snack.Id" class="link2">
                                        <span>
                                            <span class="button">Удалить</span>
                                        </span>
                                    </a>
                                </div>
                            </li>
                        }
                    </ul>
                }
            </div>
        </div>
    </div>
</div>