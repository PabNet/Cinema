@page
@model Cinema.View.Pages.CinemaHallsModel

@{
    ViewData["Title"] = Model.Title;
}

<div class="line-hor"></div>
<div class="box">
    <div class="border-right">
        <div class="border-left">
            <div class="inner">
                <h3 style="text-align: center">Список <span>кинозалов</span></h3>
                <div style="margin-left: 43%;" class="wrapper">
                    <a href="/CinemaHallForm/" class="link2">
                        <span>
                            <span>Добавить</span>
                        </span>
                    </a>
                </div>
                @if (Model.CinemaHalls.Count != default)
                {
                    <ul class="list">
                        @foreach (var cinemaHall in Model.CinemaHalls)
                        {
                            <li>
                                <a>@cinemaHall.Name</a><br/>
                                @cinemaHall.Description
                                <div>
                                    <a href="/CinemaHallForm/?id=@cinemaHall.Id" class="link2" style="margin-right: 20px">
                                        <span>
                                            <span class="button">Изменить</span>
                                        </span>
                                    </a>
                                </div>
                                <div>
                                    <a href="/CinemaHalls/?handler=Delete&id=@cinemaHall.Id" class="link2">
                                        <span>
                                            <span class="button">Удалить</span>
                                        </span>
                                    </a>
                                </div>
                            </li>
                        }
                    </ul>
                }
            </div>
        </div>
    </div>
</div>