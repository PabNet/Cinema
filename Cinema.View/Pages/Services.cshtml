@page
@model Cinema.View.Pages.ServicesModel

@{
    ViewData["Title"] = Model.Title;
}

<div class="line-hor"></div>
<div class="box">
    <div class="border-right">
        <div class="border-left">
            <div class="inner">
                <h3 style="text-align: center">Список <span>сервисов</span></h3>
                <div style="margin-left: 43%;" class="wrapper">
                    <a href="/ServiceForm/" class="link2">
                        <span>
                            <span>Добавить</span>
                        </span>
                    </a>
                </div>
                @if (Model.Services.Count != default)
                {
                    <ul class="list">
                        @foreach (var service in Model.Services)
                        {
                            <li>
                                <a>@service.Name</a><br/>
                                @service.Description
                                <div>
                                    <a href="/ServiceFrom/?id=@service.Id" class="link2" style="margin-right: 20px">
                                        <span>
                                            <span class="button">Изменить</span>
                                        </span>
                                    </a>
                                </div>
                                <div>
                                    <a href="/Services/?id=@service.Id" class="link2">
                                        <span>
                                            <span class="button">Удалить</span>
                                        </span>
                                    </a>
                                </div>
                            </li>
                        }
                    </ul>
                }
            </div>
        </div>
    </div>
</div>